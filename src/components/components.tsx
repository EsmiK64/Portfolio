import styled from "styled-components";

const Heading = styled.h1`
  color: red;
`;

const PathContainer = styled.div`
  position: absolute;
  left: 0;
  bottom: 0;
  height: 70vh;
  width: 70vw;
`;

export default {
  Heading,
  PathContainer,
};